{
  "name": "odibrick",
  "version": "1.0.0",
  "private": true,
  "description": "Odibrick — an end-to-end property rental and transaction platform by Cambliss Pvt. Ltd.",
  "workspaces": ["apps/api", "apps/web", "packages/*"],
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "install:all": "npm install --workspaces --include-workspace-root",
    "dev": "npm run dev --workspace=@odibrick/api & npm run dev --workspace=@odibrick/web",
    "build": "npm run build --workspace=@odibrick/api && npm run build --workspace=@odibrick/web",
    "test": "npm run test --workspace=@odibrick/api",
    "lint": "npm run lint --workspace=@odibrick/api && npm run lint --workspace=@odibrick/web",
    "db:migrate": "node database/migrate.js",
    "db:status": "node database/migrate.js --status",
    "db:seed": "node database/seed/seed.js",
    "db:seed:purge": "node database/seed/seed.js --purge",
    "start:api": "node apps/api/dist/main.js",
    "start:web": "npm run start --workspace=@odibrick/web"
  }
}
